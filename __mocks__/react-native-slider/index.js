const React = require('react');
const { View } = require('react-native');

const Slider = ({ value, onValueChange, minimumValue, maximumValue, style }) => {
  return React.createElement(View, { style });
};

module.exports = Slider;
